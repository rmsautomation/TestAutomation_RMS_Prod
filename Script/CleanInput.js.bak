function CleanInput(){
  var textbox = Aliases.browser.pageQms.history_quoteid;  
textbox.SetText("");
}

function CleanInputQuoteCreator(){
  var textbox = Aliases.browser.pageQms.quoteCreator;  
textbox.SetText("");
}